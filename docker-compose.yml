version: '3.8'

services:
  app:
    build: .
    ports:
      - "8000:80"
    volumes:
      - ".:/var/www/html"
    depends_on:
      - db
    environment:
      DB_HOST: db
      DB_PORT: 3306
      DB_DATABASE: php_js_mysql_lab
      DB_USERNAME: labuser
      DB_PASSWORD: labpassword

  db:
    image: mysql:8.0
    ports:
      - "3306:3306"
    environment:
      MYSQL_ROOT_PASSWORD: rootpassword
      MYSQL_DATABASE: php_js_mysql_lab
      MYSQL_USER: labuser
      MYSQL_PASSWORD: labpassword
    volumes:
      - mysql_data:/var/lib/mysql
      - ./database/migrations:/docker-entrypoint-initdb.d

  phpmyadmin:
    image: phpmyadmin:latest
    ports:
      - "8080:80"
    environment:
      PMA_HOST: db

volumes:
  mysql_data:
